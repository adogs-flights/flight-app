name: Deploy Flight App

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read
  packages: write

concurrency:
  group: flight-app-production
  cancel-in-progress: false

jobs:
  # 변경 사항 감지 (Path-based filtering)
  changes:
    runs-on: ubuntu-latest
    outputs:
      frontend: ${{ steps.filter.outputs.frontend }}
      backend: ${{ steps.filter.outputs.backend }}
      config: ${{ steps.filter.outputs.config }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            frontend:
              - 'frontend/**'
            backend:
              - 'backend/**'
            config:
              - 'docker-compose.yml'
              - 'nginx/**'
              - 'scripts/prune-deployment-images.sh'
              - 'scripts/deploy-vault.sh'
              - '.github/workflows/deploy.yml'

  # 프론트엔드 빌드 (병렬 실행 & 캐싱 적용)
  build-frontend:
    needs: changes
    if: needs.changes.outputs.frontend == 'true'
    # aarch64(arm64) 서버에 배포하므로 네이티브 ARM64 러너에서 빌드한다.
    # 예전엔 amd64 러너 + QEMU 에뮬레이션으로 arm64를 빌드해 느렸다.
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          platforms: linux/arm64
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/flight-frontend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # 백엔드 빌드 (병렬 실행 & 캐싱 적용)
  build-backend:
    needs: changes
    if: needs.changes.outputs.backend == 'true' || needs.changes.outputs.frontend == 'true' || needs.changes.outputs.config == 'true'
    # aarch64(arm64) 서버에 배포하므로 네이티브 ARM64 러너에서 빌드한다.
    # 예전엔 amd64 러너 + QEMU 에뮬레이션으로 arm64를 빌드해 느렸다.
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          platforms: linux/arm64
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/flight-backend:latest
            ghcr.io/${{ github.repository_owner }}/flight-backend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # 통합 배포 (빌드 성공 또는 설정 변경 시 실행)
  deploy:
    needs: [changes, build-frontend, build-backend]
    if: |
      always() &&
      needs.build-backend.result == 'success' &&
      (needs.build-frontend.result == 'success' || needs.build-frontend.result == 'skipped')
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME_FRONTEND: ghcr.io/${{ github.repository_owner }}/flight-frontend
      IMAGE_NAME_BACKEND: ghcr.io/${{ github.repository_owner }}/flight-backend
    steps:
      - uses: actions/checkout@v4
      - name: Test scoped image cleanup
        run: bash scripts/prune-deployment-images.test.sh
      - name: Test Vault deployment failure boundaries
        run: node --test scripts/deploy-vault.test.mjs
      - name: Copy deployment files
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.ORACLE_HOST }}
          username: ${{ secrets.ORACLE_USER }}
          key: ${{ secrets.ORACLE_KEY }}
          port: ${{ secrets.ORACLE_PORT }}
          source: "docker-compose.yml,nginx,frontend/public/privacy.html,frontend/public/terms.html,scripts/prune-deployment-images.sh,scripts/deploy-vault.sh"
          target: "/home/ubuntu/flight-app"

      - name: Deploy via SSH (Robust)
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.ORACLE_HOST }}
          username: ${{ secrets.ORACLE_USER }}
          key: ${{ secrets.ORACLE_KEY }}
          port: ${{ secrets.ORACLE_PORT }}
          envs: IMAGE_NAME_FRONTEND, IMAGE_NAME_BACKEND
          script: |
            set -euo pipefail
            
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            
            cd /home/ubuntu/flight-app
            
            # Host .env contains non-secret configuration only; never restore ENV_FILE.
            test -f .env
            export FLIGHT_BACKEND_TAG='${{ github.sha }}'
            export FLIGHT_RUNTIME_GID
            FLIGHT_RUNTIME_GID=$(getent group flight-runtime | cut -d: -f3)
            [[ "$FLIGHT_RUNTIME_GID" =~ ^[0-9]+$ ]]
            
            # 환경 변수 명시적 전달 및 이미지 갱신
            export IMAGE_NAME_FRONTEND=$IMAGE_NAME_FRONTEND
            export IMAGE_NAME_BACKEND=$IMAGE_NAME_BACKEND

            docker network inspect shared-infra >/dev/null

            FRONTEND_CONTAINER=$(docker compose ps -q flight-frontend)
            BACKEND_CONTAINER=$(docker compose ps -q flight-backend)
            if [ -n "$FRONTEND_CONTAINER" ] && [ -n "$BACKEND_CONTAINER" ]; then
              bash scripts/prune-deployment-images.sh snapshot \
                .deploy-image-state.tsv \
                "$IMAGE_NAME_FRONTEND=$FRONTEND_CONTAINER" \
                "$IMAGE_NAME_BACKEND=$BACKEND_CONTAINER"
            else
              rm -f .deploy-image-state.tsv
              echo 'One or more previous service containers are absent; image cleanup state was not created.'
            fi

            bash scripts/deploy-vault.sh "$FLIGHT_BACKEND_TAG"

            for attempt in $(seq 1 30); do
              if curl --fail --silent --show-error -H 'Host: adogs-ticket.shop' http://127.0.0.1:18091/ >/dev/null; then
                break
              fi
              if [ "$attempt" -eq 30 ]; then
                docker compose ps
                exit 1
              fi
              sleep 2
            done

            curl --fail --silent --show-error --location --max-redirs 3 \
              --retry 10 --retry-delay 3 \
              https://adogs-ticket.shop/ >/dev/null

            if [ -f .deploy-image-state.tsv ]; then
              FRONTEND_CONTAINER=$(docker compose ps -q flight-frontend)
              BACKEND_CONTAINER=$(docker compose ps -q flight-backend)
              bash scripts/prune-deployment-images.sh cleanup \
                .deploy-image-state.tsv \
                "$IMAGE_NAME_FRONTEND=$FRONTEND_CONTAINER" \
                "$IMAGE_NAME_BACKEND=$BACKEND_CONTAINER"
            else
              echo 'Image cleanup skipped: no previous deployment state.'
            fi
