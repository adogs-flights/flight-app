# Gemini Project Instructions

본 파일은 Gemini CLI가 이 프로젝트를 이해하고 작업을 수행하는 데 필요한 핵심 지침과 참조 문서를 정의합니다. 모든 작업 시 아래 문서들의 내용을 우선적으로 고려하며, 프로젝트의 컨벤션과 로드맵을 준수합니다.

## 📋 핵심 참조 문서

1. **[project.md](./project.md)**
   - **내용**: 프로젝트 아키텍처 개요 및 핵심 하네스 문서(`docs/`) 인덱스.
   - **용도**: 시스템의 전체적인 구조와 데이터 흐름을 파악할 때 참조합니다.

2. **[docs/](./docs/) 하네스 문서군 (Local Laws)**
   - **중요**: 이 문서들은 `.gitignore`에 등록되어 있으나, Gemini는 작업 시 반드시 읽어야 한다.
   - **검색 지침**: 하네스 파일 검색 시 `list_directory`나 `glob` 도구의 `respect_git_ignore` 파라미터를 `false`로 설정하여 강제로 로드한다.
   - `backend.md`: 백엔드 스택 및 DB 모델 명세.
   - `frontend.md`: 프론트엔드 UI/UX 및 컴포넌트 규칙.
   - `security.md`: 권한 제어 및 보안 정책.

3. **[issue.md](./issue.md)**
   - **내용**: 프로젝트의 버전별 이슈와 상세 로드맵.
   - **용도**: 특정 기능 구현의 배경이나 향후 계획된 변경 사항을 확인할 때 참조합니다.

4. **[todo.md](./todo.md)**
   - **내용**: 현재 수행 중인 작업의 상세 체크리스트와 **하네스 실행 기록**.
   - **용도**: 작업의 우선순위를 관리하고 진행 상태를 추적하며, 린트 검사 결과를 기록합니다.

## 🛠 작업 원칙 (Harness Engineering)

### 0. 실행 승인 (Crucial: Explicit Approval)
- **무단 수정 금지**: 사용자의 명시적인 **"작업을 시작하라"** 또는 **"승인한다"**는 명령이 있기 전까지는 어떠한 코드도 직접 수정하거나 작성하지 않는다.
- **선 계획 후 승인**: 모든 작업은 코드를 작성하기 전 구체적인 계획(Strategy)을 먼저 수립하여 `todo.md`에 반영하고, 이를 사용자에게 보고하여 허락을 득한 후 실행한다.

### 1. 하네스 기반 표준 작업 순서 (Standard Procedure)
Gemini는 모든 요청에 대해 아래 5단계를 엄격히 준수한다.

1.  **[연구: Research]**: `issue.md`에서 이슈 배경을 확인하고, `docs/` 폴더 내 관련 하네스 문서(`backend.md`, `frontend.md`, `security.md`)를 읽어 제약 사항을 파악한다.
2.  **[전략: Strategy]**: 분석 내용을 바탕으로 `todo.md`에 작업을 설계한다. 이때 [참조 하네스]와 [준수 보안 규칙]을 반드시 명시한 후 **사용자의 승인을 대기한다.**
3.  **[실행: Act]**: **사용자의 승인이 떨어진 후**, `todo.md` 계획에 따라 코드를 수정한다. 불필요한 파일은 읽지 않는 `Lazy Loading`을 실천한다.
... (이하 기존 내용 유지)
4.  **[검증: Validate]**: 수정 직후 반드시 `ruff check` (Backend) 또는 `npm run lint` (Frontend)를 실행한다. 에러 발생 시 사용자에게 보고하기 전 스스로 수정하여 'Clean 상태'를 유지한다.
5.  **[진화: Evolve]**: 작업 중 발견된 반복적인 실수나 새로운 기술적 제약은 린트 설정(`[AI_HARNESS_RULES]`) 혹은 `docs/` 문서의 [주의 사항] 섹션에 즉시 반영한다.

### 2. Local Harness (프로젝트 제약 준수)
- `docs/` 내의 명세서를 프로젝트의 '법전'으로 간주한다. 코드를 수정할 때 해당 모듈의 명세를 최우선으로 준수한다.
... (이하 기존 내용 유지)

### 2. Issue Harness (맥락 연결)
- 모든 작업은 `issue.md`의 이슈 번호와 연결되어야 한다. 작업 시작 전 해당 이슈의 배경(v1.1 ~ v2.1)을 반드시 복기한다.

### 3. 지연 로딩 (Lazy Loading Context)
- 관련 없는 파일(이미지, 로그, 타 모듈 소스)은 절대 읽지 않는다. 작업 범위에 명시된 파일과 해당 모듈의 하네스 문서만 읽어 속도를 최적화한다.

### 4. 독립 감사 시스템 (Separation of Powers & AI Auditor)
- **검수 강제화**: 코드 수정 직후, 로컬 린트(`ruff check` / `npm run lint`)와 함께 반드시 **AI Auditor 스킬(`.agents/skills/ai_audit.py`)**을 실행하여 독립적인 검수를 받아야 한다.
- **판결 기준**: AI Auditor는 프로젝트의 헌법(`docs/` 폴더 내 규칙들)을 기준으로 판결한다. 
- **수정 루프**: 결과가 `[PASS]`가 아니라 `[AI-LINT-ERROR]`라면, 사용자의 개입 없이 즉시 코드를 자가 수정하고 재검수를 요청해야 한다. 에러가 발생한 채로 작업을 종료하거나 보고해서는 안 된다.

### 5. 자가 진화 (Self-Evolving Harness)
- **실수 인지 및 패턴 추출**: 사용자의 피드백을 받거나 린트 에러를 해결했을 때, 해당 오류의 기술적/논리적 원인을 분석하여 '재발 방지 규칙'을 도출한다.
- **선제적 하네스 등록**: 문제 발생 시 사용자의 요청이 없더라도 즉시 원인을 분석하여 관련 하네스 문서(`docs/*.md`)에 기록하고, 가능할 경우 린트 규칙(AI_HARNESS_RULES)에 반영한다.
- **시스템 반영**: 
    - `pyproject.toml` 또는 `eslint.config.js`의 `[AI_HARNESS_RULES]` 섹션에 새로운 규칙을 즉시 추가한다.
    - 린트로 자동화가 불가능한 논리적 실수는 `docs/` 내 관련 문서의 [주의 사항] 섹션에 기록한다.
- **보고 양식**: 작업 완료 보고 시 "린트 검사 결과: [Pass/Fail]" 정보를 반드시 포함하며, 새로 추가된 하네스 규칙이 있다면 함께 보고한다.
